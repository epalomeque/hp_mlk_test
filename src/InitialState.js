// import { characters } from './/components/characters_data';
// import { getFavorites } from './components/Functions';

// hp_reducer
export const InitialState = {
    // characters: characters,
    // favorites: [getFavorites(characters)],
    favorites: [],
    toggleStudents: true,
    toggleStaff: true,
    printCharCard:true,
    max_favs:5,
    addCharId: false, 
    favMenuId: false
  };

export default InitialState;